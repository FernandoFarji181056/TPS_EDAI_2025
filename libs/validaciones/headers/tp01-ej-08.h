#ifndef SUBCONJUNTOS_H
#define SUBCONJUNTOS_H

#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

// Declaraciones de funciones
/*
bool validarEntero(char* cadena);
int sumaConjunto(int* conjunto, int longitud, int indice);
void imprimirConjunto(int* conjunto, int longitud, int indice);
void subconjuntos(int* conjunto,int longitud, int suma_buscada, int indiceConjunto, 
                 int* subconjunto, int indiceSubconjunto, int longitudSubconjunto);
*/              
void ejecutarSubconjuntos();  // Esta será la función principal que reemplaza al main

#endif